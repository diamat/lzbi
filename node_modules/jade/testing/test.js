
/**
 * Module dependencies.
 */

var jade = require('./../lib/jade')
  , Compiler = jade.Compiler
  , nodes = jade.nodes;



var locals = {
    first: 'tj'
  , last: 0
  , pets: ['tobi', 'loki', 'jane']
  , owners: ['tj', 'taylor']
  , block: function(name, fn){
    fn();
  }
};

var path = __dirname + '/test.jade';
jade.renderFile(path, { debug: false, filename: path, locals: locals }, function(err, html){
  if (err) throw err;
  console.log(html);
});